import { useState } from 'react';
import { useForm } from 'react-hook-form';
import { z } from 'zod';
import { zodResolver } from '@hookform/resolvers/zod';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { FilePlus2, Printer, Search } from 'lucide-react';
import { Link, useLocation, useNavigate, useParams } from 'react-router-dom';
import { Empty, ErrorBox, Field, Loading, PageTitle } from '../../components/ui/Feedback';
import { PrintPreviewDialog } from '../../components/ui/Dialog';
import { Input } from '../../components/ui/Input';
import { Select } from '../../components/ui/Select';
import { dateOnly } from '../../lib/format';
import { api } from '../../services/api';
import { useSession } from '../../app/session';
import { invalidateAll, useDb } from '../../app/queries';
import { navigateWithLoading } from '../../app/routeLoading';
const documentSchema = z.object({ typeId: z.string().min(1, 'Selecione o tipo.'), subject: z.string().trim().min(1, 'Informe o assunto.'), body: z.string().trim().min(1, 'Informe o corpo.'), recipientPersonId: z.string().optional() });
export function Documents() {
    const ctx = useSession();
    const [search, setSearch] = useState('');
    const [typeId, setTypeId] = useState('');
    const { data, isLoading, error } = useQuery({ queryKey: ['documents', ctx.userId, ctx.activeUnitId, search, typeId], queryFn: () => api.listDocuments(ctx, search, typeId) });
    if (isLoading)
        return <Loading variant="list"/>;
    if (error || !data)
        return <ErrorBox error={error}/>;
    return <><PageTitle title="Documentos" action={<Link className="btn-primary" to="/documentos/novo"><FilePlus2 size={16}/>Redigir documento</Link>}/><div className="panel mb-4 flex flex-col gap-2 p-3 sm:flex-row"><label className="relative flex-1"><Search className="absolute left-3 top-2.5 text-slate-400" size={17}/><Input aria-label="Buscar documentos" className="field !mt-0 pl-9" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Buscar número ou assunto"/></label><Select aria-label="Filtrar por tipo de documento" className="field !mt-0 sm:w-52" value={typeId} onChange={(event) => setTypeId(event.target.value)}><option value="">Todos os tipos</option>{data.db.documentTypes.map((type) => <option key={type.id} value={type.id}>{type.name}</option>)}</Select></div>{data.items.length ? <div className="panel overflow-hidden"><div className="space-y-3 p-3 md:hidden">{data.items.map((document) => <article className="rounded-md border p-4" key={document.id}><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="font-mono text-xs text-slate-500 dark:text-slate-400">{document.number}</p><h2 className="mt-1 font-semibold">{document.subject}</h2></div><Link className="btn-secondary !py-1" to={`/documentos/${document.id}`}>Abrir</Link></div><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><div><dt className="label">Tipo</dt><dd className="mt-1">{data.db.documentTypes.find((type) => type.id === document.typeId)?.name}</dd></div><div><dt className="label">Data</dt><dd className="mt-1">{dateOnly(document.createdAt)}</dd></div><div className="col-span-2"><dt className="label">Protocolo</dt><dd className="mt-1">{document.protocolId ? <Link className="text-public-700 underline" to={`/protocolos/${document.protocolId}`}>{data.db.protocols.find((protocol) => protocol.id === document.protocolId)?.number}</Link> : 'Avulso'}</dd></div></dl></article>)}</div><div className="hidden overflow-x-auto md:block"><table className="w-full min-w-[700px] text-sm"><thead><tr><th scope="col" className="table-head">Número</th><th scope="col" className="table-head">Assunto</th><th scope="col" className="table-head">Tipo</th><th scope="col" className="table-head">Protocolo</th><th scope="col" className="table-head">Data</th><th scope="col" className="table-head"><span className="sr-only">Ações</span></th></tr></thead><tbody>{data.items.map((document) => <tr className="border-t" key={document.id}><td className="px-3 py-3 font-mono text-xs">{document.number}</td><td className="px-3 py-3 font-semibold">{document.subject}</td><td className="px-3 py-3">{data.db.documentTypes.find((type) => type.id === document.typeId)?.name}</td><td className="px-3 py-3">{document.protocolId ? <Link className="text-public-700 underline" to={`/protocolos/${document.protocolId}`}>{data.db.protocols.find((protocol) => protocol.id === document.protocolId)?.number}</Link> : 'Avulso'}</td><td className="px-3 py-3">{dateOnly(document.createdAt)}</td><td className="px-3 py-3"><Link className="btn-secondary !py-1" to={`/documentos/${document.id}`}>Abrir</Link></td></tr>)}</tbody></table></div></div> : <Empty title="Nenhum documento encontrado" detail="Comece redigindo um documento simples."/>}</>;
}
export function NewDocument() { const navigate = useNavigate(); const ctx = useSession(); const queryClient = useQueryClient(); const { data: db, isLoading } = useDb(); const protocolId = new URLSearchParams(useLocation().search).get('protocolId') ?? undefined; const form = useForm<z.infer<typeof documentSchema>>({ resolver: zodResolver(documentSchema), defaultValues: { typeId: '', subject: '', body: '' } }); const create = useMutation({ mutationFn: (value: z.infer<typeof documentSchema>) => api.createDocument(ctx, { ...value, protocolId }), onSuccess: (document) => { invalidateAll(queryClient); navigateWithLoading(navigate, `/documentos/${document.id}`); } }); if (isLoading || !db)
    return <Loading variant="detail"/>; const protocol = protocolId ? db.protocols.find((item) => item.id === protocolId) : undefined; return <><PageTitle title="Redigir documento"/><form className="mx-auto max-w-7xl space-y-5" onSubmit={form.handleSubmit((value) => create.mutate(value))}><section className="panel p-5"><div className="grid gap-4 sm:grid-cols-2"><Field label="Tipo de documento *" error={form.formState.errors.typeId?.message}><Select className="field" {...form.register('typeId')} value={form.watch('typeId')} onChange={(event) => form.setValue('typeId', event.target.value, { shouldValidate: true })}><option value="">Selecione</option>{db.documentTypes.filter((type) => type.active).map((type) => <option key={type.id} value={type.id}>{type.name}</option>)}</Select></Field><Field label="Destinatário"><Select className="field" {...form.register('recipientPersonId')} value={form.watch('recipientPersonId') ?? ''} onChange={(event) => form.setValue('recipientPersonId', event.target.value)}><option value="">Sem destinatário</option>{db.people.filter((person) => person.active).map((person) => <option key={person.id} value={person.id}>{person.name}</option>)}</Select></Field>{protocol && <Field label="Protocolo vinculado"><Input className="field bg-slate-50" value={`${protocol.number} — ${protocol.subject}`} readOnly/></Field>}<Field label="Assunto *" error={form.formState.errors.subject?.message}><Input className="field" {...form.register('subject')}/></Field></div><Field label="Corpo do documento *" error={form.formState.errors.body?.message}><textarea className="field min-h-[340px] leading-7" placeholder="Escreva o texto do documento. As quebras de linha serão preservadas." {...form.register('body')}/></Field></section>{create.error && <ErrorBox error={create.error}/>}<div className="flex justify-end gap-2"><Link className="btn-secondary" to="/documentos">Cancelar</Link><button className="btn-primary" disabled={create.isPending}>Salvar documento</button></div></form></>; }
export function DocumentDetail() { const { id = '' } = useParams(); const ctx = useSession(); const [printPreview, setPrintPreview] = useState(false); const { data, isLoading } = useQuery({ queryKey: ['documents', ctx.userId, ctx.activeUnitId], queryFn: () => api.listDocuments(ctx) }); if (isLoading || !data)
    return <Loading variant="detail"/>; const document = data.items.find((item) => item.id === id); if (!document)
    return <><PageTitle title="Registro não encontrado"/><Empty title="Documento não encontrado" detail="Ele pode não estar visível no seu contexto."/></>; return <><PageTitle eyebrow={document.number} title={document.subject} action={<button className="btn-secondary no-print" onClick={() => setPrintPreview(true)}><Printer size={16}/>Prévia de impressão</button>}/><article className="panel mx-auto max-w-7xl p-7 print-shell"><header className="border-b pb-5"><p className="font-mono text-sm text-slate-500 dark:text-slate-400">{document.number}</p><h2 className="mt-2 text-2xl font-bold">{document.subject}</h2><p className="mt-2 text-sm text-slate-500 dark:text-slate-400">{data.db.documentTypes.find((type) => type.id === document.typeId)?.name} · {dateOnly(document.createdAt)}{document.recipientPersonId && <> · Destinatário: {data.db.people.find((person) => person.id === document.recipientPersonId)?.name ?? '—'}</>}</p></header><div className="whitespace-pre-wrap py-8 text-[15px] leading-8">{document.body}</div>{document.protocolId && <footer className="border-t pt-4 text-sm"><Link className="text-public-700 underline" to={`/protocolos/${document.protocolId}`}>Abrir protocolo vinculado</Link></footer>}</article>{printPreview && <PrintPreviewDialog title={document.number} onClose={() => setPrintPreview(false)}><header className="border-b border-slate-300 pb-5"><p className="font-mono text-sm text-slate-600">{document.number}</p><h1 className="mt-2 text-2xl font-bold">{document.subject}</h1><p className="mt-2 text-sm text-slate-600">{data.db.documentTypes.find((type) => type.id === document.typeId)?.name} · {dateOnly(document.createdAt)}{document.recipientPersonId && <> · Destinatário: {data.db.people.find((person) => person.id === document.recipientPersonId)?.name ?? '—'}</>}</p></header><div className="whitespace-pre-wrap py-8 text-[15px] leading-8">{document.body}</div>{document.protocolId && <footer className="border-t border-slate-300 pt-4 text-sm">Protocolo vinculado: {data.db.protocols.find((protocol) => protocol.id === document.protocolId)?.number ?? '—'}</footer>}</PrintPreviewDialog>}</>; }
